import 'dotenv/config';import express from'express';import OpenAI from'openai';let a=express(),o=new OpenAI();a.use(express.json());a.use(express.static('public'));a.post('/api/analisar',async(r,s)=>{let x=r.body||{},q='People Analytics. Gere relatorio markdown com nota, pontos fortes,lacunas,perguntas. Vaga:'+x.vaga+' Curriculo:'+x.curriculo;let y=await o.responses.create({model:process.env.OPENAI_MODEL||'gpt-5.5',input:q});s.json({report:y.output_text})});a.listen(process.env.PORT||3000);